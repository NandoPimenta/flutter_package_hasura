
// ignore_for_file: constant_identifier_names

enum HasuraENUM { QUERY, MUTATION, SUBSCRIPTION }
enum HasuraMutationENUM { QUERY, INSERT, UPDATE, DELETE, SUBSCRIPTION }

enum HasuraWhereEnum {
  EQ,
  GT,
  GTE,
  IN,
  ISNULL,
  LT,
  LTE,
  NEQ,
  NIN,
  ILIKE,
  IREGEX,
  LIKE,
  NILIKE,
  NIREGEX,
  NLIKE,
  NREGEX,
  NSIMILAR,
  REGEX,
  SIMILAR,
}
